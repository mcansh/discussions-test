import { Processor } from 'unified/types/ts3.4/';
import remark from 'remark';
export { setCDN } from 'shiki';
import { Plugin } from 'unified';

declare function getProcessor(options?: Options): Processor<remark.RemarkOptions>;
declare type Options = {
    resolveHref?: (href: string) => string;
};
declare function processMarkdown(content: string, options?: Options): Promise<string>;
declare let remarkCodeBlocksShiki: Plugin;

export { Options, getProcessor, processMarkdown, remarkCodeBlocksShiki };
